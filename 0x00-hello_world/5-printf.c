#include <stdio.h>
/**
 * main - main function entry point
 * Description: using printf to print the output
 * Return: 0
 */
int main(void)
{
	char a[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", a);
	return (0);
}
